{
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "shared",
    "sdk",
    "backend",
    "client-core",
    "frontend",
    "testkit"
  ],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build:frontend": "cd frontend && bun run build",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p release/test/tsconfig.json && (cd shared && bun run typecheck) && (cd sdk && bun run typecheck) && (cd backend && bun run typecheck) && (cd testkit && bun run typecheck) && (cd client-core && bun run typecheck) && (cd frontend && bun run typecheck)",
    "lint": "eslint \"**/*.{ts,svelte}\" --max-warnings=0",
    "test": "bun test",
    "test:e2e": "bun run build:frontend && bun ./node_modules/@playwright/test/cli.js test --config frontend/playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@pohunek/backend": "workspace:*",
    "@pohunek/protocol": "workspace:*",
    "@pohunek/testkit": "workspace:*",
    "@types/bun": "1.3.11",
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-svelte": "3.22.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vite": "8.1.5"
  }
}
