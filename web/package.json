{
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "shared",
    "sdk",
    "backend"
  ],
  "scripts": {
    "typecheck": "(cd shared && bun run typecheck) && (cd sdk && bun run typecheck) && (cd backend && bun run typecheck)",
    "lint": "eslint \"**/*.ts\" --max-warnings=0",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0"
  }
}
