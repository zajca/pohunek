// @generated by `cargo xtask ts generate` - do not edit.

import type { ProjectInfo } from "./ProjectInfo";
import type { ProjectWorktree } from "./ProjectWorktree";

/**
 * Result of `project.show`: the project plus its live worktrees.
 */
export type ProjectShowResult = {
  /**
   * The project record (display shape).
   */
  project: ProjectInfo;
  /**
   * Worktrees reported live by git, enriched with ownership/session info.
   */
  worktrees: Array<ProjectWorktree>;
};
