// @generated by `cargo xtask ts generate` - do not edit.

import type { SessionId } from "./SessionId";
import type { SessionRetentionHold } from "./SessionRetentionHold";
import type { SessionRetentionReason } from "./SessionRetentionReason";

/**
 * One session a retention sweep matched, whether it removed it or held it back.
 */
export type SessionRetentionCandidate = {
  /**
   * Selected session.
   */
  session_id: SessionId;
  /**
   * Rule that selected it.
   */
  reason: SessionRetentionReason;
  /**
   * Age in seconds at selection time, measured from the timestamp the rule
   * used (the session's last update, or the runtime's last connection).
   */
  age_secs: bigint;
  /**
   * Pohunek-owned worktree the removal cleans, when the session bound one.
   */
  worktree_path?: string;
  /**
   * Why the sweep kept this session although its age matched the policy.
   * `None` for a session the sweep is free to remove.
   */
  hold?: SessionRetentionHold;
  /**
   * Whether this sweep actually removed the session. Always `false` for a
   * dry run, for a held candidate, and for a candidate the sweep's removal
   * cap deferred.
   */
  removed: boolean;
};
