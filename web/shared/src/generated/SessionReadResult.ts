// @generated by `cargo xtask ts generate` - do not edit.

import type { RuntimeGeneration } from "./RuntimeGeneration";
import type { SessionReadSource } from "./SessionReadSource";
import type { TerminalWatermark } from "./TerminalWatermark";

/**
 * Bounded terminal text returned by `session.read`.
 */
export type SessionReadResult = {
  /**
   * Captured terminal text in the requested format.
   */
  text: string;
  /**
   * Source actually used to produce the capture.
   */
  source_used: SessionReadSource;
  /**
   * Worker output offset represented by this snapshot.
   */
  revision: TerminalWatermark;
  /**
   * Whether the terminal is in its alternate screen buffer.
   *
   * Recent sources fall back to visible rows when history is unavailable.
   */
  alternate_screen: boolean;
  /**
   * Effective line limit applied by the daemon.
   */
  lines_requested: number;
  /**
   * Whether any line or byte limit discarded content.
   */
  truncated: boolean;
  /**
   * PTY runtime identifier.
   */
  runtime_id: string;
  /**
   * Monotonic logical-session generation for this runtime.
   */
  runtime_generation: RuntimeGeneration;
};
