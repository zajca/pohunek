// @generated by `cargo xtask ts generate` - do not edit.

import type { OutputOffset } from "./OutputOffset";
import type { SessionInfo } from "./SessionInfo";
import type { SessionWaitReason } from "./SessionWaitReason";
import type { TerminalWatermark } from "./TerminalWatermark";

/**
 * Result returned by the bounded long-poll `session.wait` method.
 */
export type SessionWaitResult = {
  /**
   * Condition that completed the wait.
   */
  reason: SessionWaitReason;
  /**
   * Redacted current public session snapshot.
   */
  session: SessionInfo;
  /**
   * Current terminal watermark when a managed terminal is available.
   */
  terminal_watermark?: TerminalWatermark;
  /**
   * Current output cursor when a managed terminal is available.
   */
  output_offset?: OutputOffset;
};
