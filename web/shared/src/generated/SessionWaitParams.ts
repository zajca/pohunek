// @generated by `cargo xtask ts generate` - do not edit.

import type { AgentActivity } from "./AgentActivity";
import type { OutputOffset } from "./OutputOffset";
import type { SessionId } from "./SessionId";
import type { SessionRuntimeIdentity } from "./SessionRuntimeIdentity";
import type { SessionState } from "./SessionState";
import type { TerminalWatermark } from "./TerminalWatermark";

/**
 * Parameters for the bounded long-poll `session.wait` method.
 */
export type SessionWaitParams = {
  /**
   * Session to observe.
   */
  session_id: SessionId;
  /**
   * Runtime identity whose replacement should wake the waiter.
   */
  runtime?: SessionRuntimeIdentity;
  /**
   * Session update cursor in the daemon's RFC 3339 wire format.
   */
  after_updated_at?: string;
  /**
   * Terminal watermark cursor.
   */
  after_terminal_watermark?: TerminalWatermark;
  /**
   * Output cursor scoped by [`Self::runtime`].
   */
  after_output_offset?: OutputOffset;
  /**
   * Terminal states that complete the wait when observed.
   */
  states?: Array<SessionState>;
  /**
   * Agent activities that complete the wait when observed.
   */
  activities?: Array<AgentActivity>;
  /**
   * Required nonzero bounded wait duration.
   */
  timeout_ms: number;
};
