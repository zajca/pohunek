// @generated by `cargo xtask ts generate` - do not edit.

import type { ActivityRevision } from "./ActivityRevision";
import type { AgentActivity } from "./AgentActivity";
import type { SessionRuntimeIdentity } from "./SessionRuntimeIdentity";
import type { StateSource } from "./StateSource";

/**
 * Result returned by `session.input`.
 */
export type SessionInputResult = {
  /**
   * Whether the daemon accepted the input for delivery.
   */
  accepted: boolean;
  /**
   * Final detected agent activity when a wait was requested.
   */
  activity?: AgentActivity;
  /**
   * Evidence source behind [`Self::activity`] when a wait was requested.
   */
  activity_source?: StateSource;
  /**
   * Runtime that produced the activity evidence for a waited delivery.
   */
  runtime?: SessionRuntimeIdentity;
  /**
   * Exact post-submission activity revision that completed the wait.
   */
  activity_revision?: ActivityRevision;
};
