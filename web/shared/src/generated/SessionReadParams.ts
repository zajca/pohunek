// @generated by `cargo xtask ts generate` - do not edit.

import type { SessionId } from "./SessionId";
import type { SessionReadFormat } from "./SessionReadFormat";
import type { SessionReadSource } from "./SessionReadSource";

/**
 * Parameters for `session.read`.
 */
export type SessionReadParams = {
  /**
   * Session whose managed terminal should be read.
   */
  session_id: SessionId;
  /**
   * Requested source. Omission uses [`SessionReadSource::Visible`].
   */
  source?: SessionReadSource;
  /**
   * Maximum returned lines. Omission uses the protocol ceiling.
   */
  lines?: number;
  /**
   * Requested encoding. Omission uses [`SessionReadFormat::Text`].
   */
  format?: SessionReadFormat;
};
