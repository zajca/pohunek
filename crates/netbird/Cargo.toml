[package]
name = "pohunek-netbird"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "NetBird status parsing, host resolution, and bind-address validation for pohunek remote hosts."

[lib]
name = "netbird"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
