[package]
name = "pohunek-test-support"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Test-only fixture roots that satisfy the trusted filesystem and Unix socket limits on every supported host."
publish = false

[dependencies]
tempfile.workspace = true

[lints]
workspace = true
