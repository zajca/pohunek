name: Release

# Triggered by pushing a `vX.Y.Z` tag (created by `scripts/release`). First runs
# the full quality gate (fmt/clippy/test) on the tagged commit, and only if that
# passes builds the native component binaries and the self-contained web control
# center for Linux x86_64, packages them with sha256 checksums, and attaches
# everything to a GitHub Release for that tag.
#
# A tag push does NOT run the CI workflow (CI runs on branch pushes and PRs), so
# the gate is duplicated here: no binary is published from a commit that fails
# fmt, clippy, or tests.
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  gate:
    name: fmt + clippy + test
    runs-on: ubuntu-latest
    env:
      RUSTFLAGS: -D warnings
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features

      - name: Test
        run: cargo test --workspace --all-features

  docs-gate:
    name: docs check (deterministic build + secret scan)
    runs-on: ubuntu-latest
    env:
      RUSTFLAGS: -D warnings
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2
        with:
          key: docs

      - name: docs check
        run: cargo xtask docs check

  web-gate:
    name: web typecheck + lint + test + e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2
        with:
          key: release-web

      - name: Install web dependencies
        working-directory: web
        run: bun install --frozen-lockfile

      - name: Typecheck web
        working-directory: web
        run: bun run typecheck

      - name: Lint web
        working-directory: web
        run: bun run lint

      - name: Test web
        working-directory: web
        run: bun test

      - name: Build web control center
        working-directory: web
        run: bun run build:frontend

      - name: Install Playwright Chromium
        working-directory: web
        run: bunx playwright install --with-deps chromium

      - name: Test web control center
        working-directory: web
        run: bun run test:e2e

      - name: Build daemon for web e2e
        run: cargo build -p pohunek-daemon

      - name: SDK e2e against real daemon
        working-directory: web
        run: bun test sdk/test/e2e.test.ts
        env:
          POHUNEK_E2E: "1"
          POHUNEK_DAEMON_BIN: ${{ github.workspace }}/target/debug/pohunekd

      - name: Backend e2e against real daemon
        working-directory: web
        run: bun test backend/test/real-daemon.e2e.test.ts
        env:
          POHUNEK_E2E: "1"
          POHUNEK_DAEMON_BIN: ${{ github.workspace }}/target/debug/pohunekd

  web-release:
    name: Build web control center for Linux x86_64
    needs: [gate, docs-gate, web-gate]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11

      - name: Install web dependencies
        working-directory: web
        run: bun install --frozen-lockfile

      - name: Build and package web control center
        id: package
        working-directory: web
        shell: bash
        run: bash release/package.sh "${GITHUB_REF_NAME#v}"

      - name: Attach to release
        uses: softprops/action-gh-release@v2
        with:
          fail_on_unmatched_files: true
          files: |
            web/${{ steps.package.outputs.archive }}
            web/${{ steps.package.outputs.checksum }}

  build:
    name: Build ${{ matrix.component }} for ${{ matrix.target }}
    needs: [gate, docs-gate, web-gate]
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # Dynamically linked glibc build (primary target).
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
            component: cli
            archive_prefix: pohunek-cli
            binary: pohunek
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
            component: daemon
            archive_prefix: pohunek-daemon
            binary: pohunekd
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
            component: gui
            archive_prefix: pohunek-gui
            binary: pohunek-gui
          # Fully static musl build: a single self-contained binary that runs on
          # any x86_64 Linux regardless of the host glibc version.
          - target: x86_64-unknown-linux-musl
            runner: ubuntu-latest
            component: cli
            archive_prefix: pohunek-cli
            binary: pohunek
          - target: x86_64-unknown-linux-musl
            runner: ubuntu-latest
            component: daemon
            archive_prefix: pohunek-daemon
            binary: pohunekd
          - target: x86_64-unknown-linux-musl
            runner: ubuntu-latest
            component: gui
            archive_prefix: pohunek-gui
            binary: pohunek-gui
    steps:
      - uses: actions/checkout@v4

      - name: Install musl toolchain
        if: endsWith(matrix.target, '-musl')
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}-${{ matrix.component }}

      - name: Build release binaries
        run: >
          cargo build --release --locked --target ${{ matrix.target }}
          --bin pohunek --bin pohunekd --bin pohunek-gui

      - name: Build docs artifacts
        run: cargo xtask docs site

      - name: Package
        id: package
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          name="${{ matrix.archive_prefix }}-${version}-${{ matrix.target }}"
          staging="dist/${name}"
          mkdir -p "${staging}"
          bindir="target/${{ matrix.target }}/release"
          cp "${bindir}/${{ matrix.binary }}" "${staging}/"
          mkdir -p "${staging}/docs"
          cp -R target/pohunek-docs/offline "${staging}/docs/offline"
          cp target/pohunek-docs/manifest.json "${staging}/docs/manifest.json"
          for extra in README.md LICENSE; do
            if [ ! -f "${extra}" ]; then
              echo "::error file=${extra}::required release file is missing"
              exit 1
            fi
            cp "${extra}" "${staging}/"
          done
          tar -czf "dist/${name}.tar.gz" -C dist "${name}"
          ( cd dist && shasum -a 256 "${name}.tar.gz" > "${name}.tar.gz.sha256" )
          echo "archive=dist/${name}.tar.gz" >> "${GITHUB_OUTPUT}"
          echo "checksum=dist/${name}.tar.gz.sha256" >> "${GITHUB_OUTPUT}"

      - name: Attach to release
        uses: softprops/action-gh-release@v2
        with:
          fail_on_unmatched_files: true
          files: |
            ${{ steps.package.outputs.archive }}
            ${{ steps.package.outputs.checksum }}
